# Copy to .env and adjust per environment

# Base URL of the Spring Boot API (must end with /api/v1)
EXPO_PUBLIC_API_URL=http://localhost:8080/api/v1

# HTTP timeout in milliseconds
EXPO_PUBLIC_API_TIMEOUT_MS=15000
