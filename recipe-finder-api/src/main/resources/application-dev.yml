spring:
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:recipefinder}
    username: ${DB_USER:NoutSpace}
    password: ${DB_PASSWORD:123456}
  jpa:
    show-sql: true

logging:
  level:
    com.recipefinder: DEBUG
    org.springframework.security: INFO
